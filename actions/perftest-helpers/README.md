# Пакет `@salutejs/perftest-helpers`

Запускается в джобе перфтестов для PR: `.github/workflows/performance-test-pr.yml`. Перед запуском необходимо установить пакет:

```bash
npm ci
npx lerna bootstrap
```
