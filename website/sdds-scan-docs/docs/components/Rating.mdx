---
id: rating
title: Rating
---

import { PropsTable, Description } from '@site/src/components';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Rating
<Description name="Rating" />
<PropsTable name="Rating" exclude={['css']} />

## Использование
Компонент `Rating` представляет собой неинтерактивный компонент для отображения оценки.

```tsx live
import React, {useState} from 'react';
import { Rating, Button } from '@salutejs/sdds-scan';

export function App() {
    return (
        <div>
            <Rating
                view="default"
                size="m"
                value={3.8}
                helperText="Helper text"
            />
        </div>
    );
}
```

### Вид Rating
Вид Rating задаётся с помощью свойства `view`:

```tsx live
import React from 'react';
import { Rating } from '@salutejs/sdds-scan';

export function App() {
    return (
        <div style={{ display: "flex", flexDirection: "column", gap: "2rem" }}>
            <Rating
                view="default"
                size="m"
                value={3.8}
                helperText="Helper text"
            />
            <Rating
                view="accent"
                size="m"
                value={3.8}
                helperText="Helper text"
            />
        </div>
    );
}
```

### Размер Rating
Размер Rating задается с помощью свойства `size`:

```tsx live
    import React from 'react';
    import { Rating } from '@salutejs/sdds-scan';

    export function App() {
        return (
            <div style={{ display: "flex", flexDirection: "column", gap: "2rem" }}>
                <Rating
                    view="default"
                    size="m"
                    value={3.8}
                    helperText="Helper text"
                />
                <Rating
                    view="default"
                    size="s"
                    value={3.8}
                    helperText="Helper text"
                />
            </div>
        );
    }
```

### Количество иконок в Rating
Количество иконок в Rating задается с помощью свойства `iconQuantity`:

```tsx live
import React from 'react';
import { Rating } from '@salutejs/sdds-scan';

export function App() {
    return (
        <div style={{ display: "flex", flexDirection: "column", gap: "2rem" }}>
            <Rating
                view="default"
                size="m"
                value={3.8}
                iconQuantity={5}
                helperText="Helper text"
            />
            <Rating
                view="accent"
                size="m"
                value={3.8}
                iconQuantity={10}
                helperText="Helper text"
            />
            <Rating
                view="accent"
                size="m"
                value={3.8}
                iconQuantity={1}
                helperText="Helper text"
            />
        </div>
    );
}
```

### Положение значения в Rating
Положение значения в Rating задается с помощью свойства `valuePlacement`. 
Отображение значения можно выключить с помощью свойства `hasValue`:

```tsx live
import React from 'react';
import { Rating } from '@salutejs/sdds-scan';

export function App() {
    return (
        <div style={{ display: "flex", flexDirection: "column", gap: "2rem" }}>
            <Rating
                view="default"
                size="m"
                value={3.8}
                valuePlacement="before"
                helperText="Helper text"
            />
            <Rating
                view="accent"
                size="m"
                value={3.8}
                valuePlacement="after"
                helperText="Helper text"
            />
            <Rating
                view="accent"
                size="m"
                value={3.8}
                hasValue={false}
                helperText="Helper text"
            />
        </div>
    );
}
```

### Пользовтельские иконки в Rating
Пользовтельские иконки в Rating задаются с помощью свойств:
+ `iconSlot` – закрашенная иконка;
+ `iconSlotOutline` – outline иконка;
+ `iconSlotHalf` - иконка для отображения дробного значения оценки.

Для корректного применения размеров к иконкам, необходимо передавать класс `ratingClasses.customIconSizing` ("custom-icon-sizing")


```tsx live
import React from 'react';
import { IconKeyFill, IconKeyOutline, IconLockFill } from '@salutejs/plasma-icons';
import { ratingClasses, Rating } from '@salutejs/sdds-scan';

export function App() {
    return (
        <div style={{ display: "flex", flexDirection: "column", gap: "2rem" }}>
            <Rating
                view="default"
                size="m"
                value={3.8}
                helperText="Helper text"
                iconSlot={<IconKeyFill size="s" className={ratingClasses.customIconSizing} />}
                iconSlotOutline={<IconLockFill size="s" className={ratingClasses.customIconSizing} />}
                iconSlotHalf={<IconKeyOutline size="s" className={ratingClasses.customIconSizing} />}
            />
        </div>
    );
}
```