---
id: components
title: Поддержка компонентов
sidebar_position: 4
---

### TextField, TextArea, Radiobox, Checkbox и Switch

#### React Hook Form

Изначальные значения задаются в defaultValues. Возвращаемый и изначальный тип как у нативных элементов.

#### Нативная форма

Изначальные значения задаются в defaultValue через параметр в компоненте.


### Single Slider

#### React Hook Form

Изначальные значения задаются в defaultValue через параметр в компоненте. Возвращаемый и изначальный тип `number`.

#### Нативная форма

Изначальные значения задаются в defaultValue через параметр в компоненте. Возвращаемый и изначальный тип `number`.

### Double Slider

#### React Hook Form

Изначальные значения задаются в defaultValue (`number[]`) через параметр в компоненте. Возвращаемый тип `string` в формате `[MIN] - [MAX]`.

#### Нативная форма

Изначальные значения задаются в defaultValue (`number[]`) через параметр в компоненте. Возвращается два ключа с одинаковым именем. Первое значение `min`, второе `max`.
```

[
    ['textfield': 'test'],
    ...
    ['sliderdouble': 1],
    ['sliderdouble': 3],
    ...
    ['textfield2': 'test'],
]

```

### Combobox и Select

#### React Hook Form

Изначальные значения задаются в defaultValue через параметр в компоненте. Возвращаемый и изначальный тип `string`.

#### Нативная форма

Изначальные значения задаются в defaultValue через параметр в компоненте. Возвращаемый и изначальный тип `string`.


### Combobox Multiple и Select Multi

#### React Hook Form

Изначальные значения задаются в defaultValue (`string[]`) через параметр в компоненте. Возвращаемый тип `string[]`.

#### Нативная форма

Изначальные значения задаются в defaultValue (`number[]`) через параметр в компоненте. Возвращается количество выбранных элементов с ключем `name` и значением `value`.
```

[
    ['textfield': 'test'],
    ...
    ['combobox': 'test_1'],
    ['combobox': 'test_2'],
    ...
    ['select': 'test_1'],
    ['select': 'test_2'],
    ...
    ['textfield2': 'test'],
]

```

