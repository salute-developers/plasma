---
id: progress
title: Progress
---

import { PropsTable, Description } from '@site/src/components';

# Progress
<Description name="Progress" />
<PropsTable name="Progress" exclude={['css']} />

##Примеры

### Вид прогресса
Вид прогресса задается с помощью свойства `view`. Возможные значения свойства `view`:
+ `"default"` – по умолчанию;
+ `"accent"` – акцентная;
+ `"positive"` – успешное завершение;
+ `"negative"` – ошибка;

```tsx live
import React from 'react';
import { Progress } from '@salutejs/sdds-cs';

export function App() {
    return (
        <Progress value={25} view="accent" displayValue />
    );
}
```
