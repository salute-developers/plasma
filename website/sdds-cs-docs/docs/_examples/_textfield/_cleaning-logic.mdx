```tsx live
import React, { useState } from "react";
import { IconClose } from "@salutejs/plasma-icons";
import { TextField, IconButton } from '@salutejs/sdds-cs';

export function App() {
const [text, setName] = useState('');

  return (
    <div>
      <TextField
        value={text}
        label="Введите имя"
        placeholder="Например, Алексей"
        onChange={(e) => {
            setName(e.target.value);
        }}
        defaultValue=""
        contentRight={
          (text && !!text.length) && <IconButton
            size="s"
            view="clear"
            onClick={() => {
              setName("");
            }}
          >
            <IconClose color="inherit" size="s" />
          </IconButton>
        }
      />
    </div>
  );
}
```
