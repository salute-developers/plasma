---
id: card
title: Card
---

import { PropsTable, Description } from '@site/src/components';

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Card
Визуальный контейнер, который объединяет различные элементы, такие как изображения, текст и кнопки, в единый блок

<Description name="Card" />
<PropsTable name="Card" />

### Использование

Настройка размера осуществляется явным образом или в зависимости от содержимого, в том числе, за счет указания соотношения сторон.

Настройка внутренних отступов между контентом карточки осуществляется с учетом конкретного контекста использования компонента.

Компонент `Card` является базовым контейнером. `CardContent` используется для вложенного контента внутри `Card`, со своими скруглениями и имеет `overflow: hidden`.
`CardInnerContent` используется для контента вписанного в размеры `CardContent`, имеет `position: absolute`, не влияет на размер всей карточки.

### Пример с вложенным контентом

```tsx live
import React from 'react';
import { Card, CardContent, CardInnerContent, H3, TextS } from '@salutejs/plasma-homeds';

export function App() {
    const SvgBackground = (props) => (
        <svg
            xmlns="http://www.w3.org/2000/svg"
            preserveAspectRatio="xMinYMin slice"
            viewBox="1 0.5 8 8"
            {...props}
            style={{ ...props.style, background: 'var(--text-tertiary)' }}
        >
            <path stroke="#fff" strokeDasharray={1} d="M1 1h7M2 2h7M1 3h7M2 4h7M1 5h7M2 6h7M1 7h7M2 8h7" />
        </svg>
    );

    return (
        <Card orientation="horizontal" style={{ width: '250px' }}>
            <CardContent orientation='vertical' aspectRatio='1/1'>
                <CardInnerContent>
                    <div style={{ padding: '30px' }}>
                        <H3>Заголовок</H3>
                        <TextS style={{ paddingTop: '8px' }}>Как оплатить заправку бонусами СберСпасибо?</TextS>
                    </div>
                </CardInnerContent>
                <SvgBackground width="100%" height="100%" style={{ width: '100%', height: '100%' }} />
            </CardContent>
        </Card>
    );
}
```

### Пример выбранной карточки с `backgroundType="solid"`

```tsx live
import React from 'react';
import { Card, CardContent, H3, TextS } from '@salutejs/plasma-homeds';

export function App() {
    const SvgBackground = (props) => (
        <svg
            xmlns="http://www.w3.org/2000/svg"
            preserveAspectRatio="xMinYMin slice"
            viewBox="1 0.5 8 8"
            {...props}
            style={{ ...props.style, background: 'var(--text-tertiary)' }}
        >
            <path stroke="#fff" strokeDasharray={1} d="M1 1h7M2 2h7M1 3h7M2 4h7M1 5h7M2 6h7M1 7h7M2 8h7" />
        </svg>
    );

    return (
        <Card orientation="horizontal" style={{ width: '400px' }} backgroundType="solid" selected>
            <div style={{ padding: '30px' }}>
                <H3>Title</H3>
                <TextS style={{ paddingTop: '8px' }}>Description</TextS>
            </div>
            <CardContent orientation="vertical" aspectRatio="4/3">
                <SvgBackground width="100%" height="100%" style={{ width: '100%', height: '100%' }} />
            </CardContent>
        </Card>
    );
}
```

### Собранные примеры карточек

<Tabs>
    <TabItem value="s" label="S" default>
        ```tsx live
        import React from 'react';
        import { IconPlasma } from '@salutejs/plasma-icons';
        import { BodyXS, Card, Flow, IconButton } from '@salutejs/plasma-homeds';

        export function App() {
            const ImagePlaceholder = () => (
                <div
                    aria-label="Место для изображения"
                    style={{
                        width: '100%',
                        height: '100%',
                        backgroundColor: 'var(--inverse-surface-solid-primary)',
                        backgroundImage:
                            'linear-gradient(45deg, var(--inverse-surface-solid-secondary) 25%, transparent 25%), linear-gradient(-45deg, var(--inverse-surface-solid-secondary) 25%, transparent 25%), linear-gradient(45deg, transparent 75%, var(--inverse-surface-solid-secondary) 75%), linear-gradient(-45deg, transparent 75%, var(--inverse-surface-solid-secondary) 75%)',
                        backgroundPosition: '0 0, 0 0.5rem, 0.5rem -0.5rem, -0.5rem 0',
                        backgroundSize: '1rem 1rem',
                    }}
                />
            );

            const ProductCard = ({ hasInnerPaddings }: { hasInnerPaddings: boolean }) => (
                <Card
                    aria-label={
                        'Карточка S, ' +
                        (hasInnerPaddings ? 'с внутренними отступами' : 'без внутренних отступов')
                    }
                    orientation="vertical"
                    size="s"
                    style={{
                        width: '6.5rem',
                        flex: '0 0 auto',
                        alignItems: 'center',
                        gap: '0.5rem',
                    }}
                >
                    <div
                        style={{
                            position: 'relative',
                            width: '100%',
                            height: '3.5rem',
                            boxSizing: 'border-box',
                            padding: hasInnerPaddings ? '0 0.75rem' : 0,
                        }}
                    >
                        <ImagePlaceholder />
                        <div
                            style={{
                                position: 'absolute',
                                top: '0.0625rem',
                                left: 0,
                                right: '2.5rem',
                                zIndex: 1,
                                display: 'flex',
                                justifyContent: 'center',
                                overflow: 'hidden',
                            }}
                        />
                        <IconButton
                            aria-label="Открыть карточку"
                            size="s"
                            style={{ position: 'absolute', right: 0, bottom: 0, zIndex: 1 }}
                        >
                            <IconPlasma color="inherit" size="s" />
                        </IconButton>
                    </div>
                    <BodyXS
                        style={{
                            width: '100%',
                            minHeight: '0.875rem',
                            overflow: 'hidden',
                            color: 'var(--text-primary)',
                            textAlign: 'center',
                            textOverflow: 'ellipsis',
                            whiteSpace: 'nowrap',
                        }}
                    >
                        Title
                    </BodyXS>
                </Card>
            );

            return (
                <Flow mainAxisGap="1.25rem" style={{ padding: '0.625rem', overflowX: 'auto' }}>
                    <ProductCard hasInnerPaddings />
                    <ProductCard hasInnerPaddings={false} />
                </Flow>
            );
        }
        ```
    </TabItem>

    <TabItem value="m" label="M">
        ```tsx live
        import React from 'react';
        import { IconPlasma } from '@salutejs/plasma-icons';
        import { BodyXS, Card, Flow, IconButton, Numbers } from '@salutejs/plasma-homeds';

        export function App() {
            const ImagePlaceholder = () => (
                <div
                    aria-label="Место для изображения"
                    style={{
                        width: '100%',
                        height: '100%',
                        backgroundColor: 'var(--inverse-surface-solid-primary)',
                        backgroundImage:
                            'linear-gradient(45deg, var(--inverse-surface-solid-secondary) 25%, transparent 25%), linear-gradient(-45deg, var(--inverse-surface-solid-secondary) 25%, transparent 25%), linear-gradient(45deg, transparent 75%, var(--inverse-surface-solid-secondary) 75%), linear-gradient(-45deg, transparent 75%, var(--inverse-surface-solid-secondary) 75%)',
                        backgroundPosition: '0 0, 0 0.5rem, 0.5rem -0.5rem, -0.5rem 0',
                        backgroundSize: '1rem 1rem',
                    }}
                />
            );

            const ProductCard = ({ hasInnerPaddings }: { hasInnerPaddings: boolean }) => (
                <Card
                    aria-label={
                        'Карточка M, ' +
                        (hasInnerPaddings ? 'с внутренними отступами' : 'без внутренних отступов')
                    }
                    orientation="vertical"
                    size="m"
                    style={{
                        width: '6.5rem',
                        flex: '0 0 auto',
                        alignItems: 'center',
                        gap: '0.5rem',
                    }}
                >
                    <div
                        style={{
                            position: 'relative',
                            width: '100%',
                            height: '5rem',
                            boxSizing: 'border-box',
                            padding: hasInnerPaddings ? '0 0.75rem' : 0,
                        }}
                    >
                        <ImagePlaceholder />
                        <div
                            style={{
                                position: 'absolute',
                                top: '1.75rem',
                                left: 0,
                                right: 0,
                                zIndex: 1,
                                display: 'flex',
                                justifyContent: 'center',
                                overflow: 'hidden',
                            }}
                        >
                            <Numbers value={-28.8} unit="degree" size="xs" />
                        </div>
                        <IconButton
                            aria-label="Открыть карточку"
                            size="s"
                            style={{ position: 'absolute', right: 0, bottom: 0, zIndex: 1 }}
                        >
                            <IconPlasma color="inherit" size="s" />
                        </IconButton>
                    </div>
                    <BodyXS
                        style={{
                            width: '100%',
                            minHeight: '0.875rem',
                            overflow: 'hidden',
                            color: 'var(--text-primary)',
                            textAlign: 'center',
                            textOverflow: 'ellipsis',
                            whiteSpace: 'nowrap',
                        }}
                    >
                        Title
                    </BodyXS>
                </Card>
            );

            return (
                <Flow mainAxisGap="1.25rem" style={{ padding: '1.25rem', overflowX: 'auto' }}>
                    <ProductCard hasInnerPaddings />
                    <ProductCard hasInnerPaddings={false} />
                </Flow>
            );
        }
        ```
    </TabItem>

    <TabItem value="l" label="L">
        ```tsx live
        import React from 'react';
        import { IconPlasma } from '@salutejs/plasma-icons';
        import { BodyXS, Card, Flow, IconButton, Numbers } from '@salutejs/plasma-homeds';

        export function App() {
            const ImagePlaceholder = () => (
                <div
                    aria-label="Место для изображения"
                    style={{
                        width: '100%',
                        height: '100%',
                        backgroundColor: 'var(--inverse-surface-solid-primary)',
                        backgroundImage:
                            'linear-gradient(45deg, var(--inverse-surface-solid-secondary) 25%, transparent 25%), linear-gradient(-45deg, var(--inverse-surface-solid-secondary) 25%, transparent 25%), linear-gradient(45deg, transparent 75%, var(--inverse-surface-solid-secondary) 75%), linear-gradient(-45deg, transparent 75%, var(--inverse-surface-solid-secondary) 75%)',
                        backgroundPosition: '0 0, 0 0.5rem, 0.5rem -0.5rem, -0.5rem 0',
                        backgroundSize: '1rem 1rem',
                    }}
                />
            );

            const ProductCard = ({ hasInnerPaddings }: { hasInnerPaddings: boolean }) => (
                <Card
                    aria-label={
                        'Карточка L, ' +
                        (hasInnerPaddings ? 'с внутренними отступами' : 'без внутренних отступов')
                    }
                    orientation="vertical"
                    size="l"
                    style={{
                        width: hasInnerPaddings ? '6.5rem' : '5.875rem',
                        flex: '0 0 auto',
                        alignItems: 'center',
                        gap: '0.5rem',
                    }}
                >
                    <div
                        style={{
                            position: 'relative',
                            width: '100%',
                            height: '6.5rem',
                            boxSizing: 'border-box',
                            padding: hasInnerPaddings ? '0 0.75rem' : 0,
                        }}
                    >
                        <ImagePlaceholder />
                        <div
                            style={{
                                position: 'absolute',
                                top: '1.0625rem',
                                left: 0,
                                right: 0,
                                zIndex: 1,
                                display: 'flex',
                                justifyContent: 'center',
                                overflow: 'hidden',
                            }}
                        >
                            <Numbers value={-28.8} unit="degree" size="xs" />
                        </div>
                        <IconButton
                            aria-label="Открыть карточку"
                            size="s"
                            style={{ position: 'absolute', right: 0, bottom: 0, zIndex: 1 }}
                        >
                            <IconPlasma color="inherit" size="s" />
                        </IconButton>
                    </div>
                    <BodyXS
                        style={{
                            width: '100%',
                            minHeight: '0.875rem',
                            overflow: 'hidden',
                            color: 'var(--text-primary)',
                            textAlign: 'center',
                            textOverflow: 'ellipsis',
                            whiteSpace: 'nowrap',
                        }}
                    >
                        Title
                    </BodyXS>
                </Card>
            );

            return (
                <Flow mainAxisGap="1.25rem" style={{ padding: '0.625rem', overflowX: 'auto' }}>
                    <ProductCard hasInnerPaddings />
                    <ProductCard hasInnerPaddings={false} />
                </Flow>
            );
        }
        ```
    </TabItem>

    <TabItem value="xl" label="XL">
        ```tsx live
        import React from 'react';
        import { IconPlasma } from '@salutejs/plasma-icons';
        import { BodyXS, Card, Flow, IconButton, Numbers } from '@salutejs/plasma-homeds';

        export function App() {
            const ImagePlaceholder = () => (
                <div
                    aria-label="Место для изображения"
                    style={{
                        width: '100%',
                        height: '100%',
                        backgroundColor: 'var(--inverse-surface-solid-primary)',
                        backgroundImage:
                            'linear-gradient(45deg, var(--inverse-surface-solid-secondary) 25%, transparent 25%), linear-gradient(-45deg, var(--inverse-surface-solid-secondary) 25%, transparent 25%), linear-gradient(45deg, transparent 75%, var(--inverse-surface-solid-secondary) 75%), linear-gradient(-45deg, transparent 75%, var(--inverse-surface-solid-secondary) 75%)',
                        backgroundPosition: '0 0, 0 0.5rem, 0.5rem -0.5rem, -0.5rem 0',
                        backgroundSize: '1rem 1rem',
                    }}
                />
            );

            const ProductCard = ({ hasInnerPaddings }: { hasInnerPaddings: boolean }) => (
                <Card
                    aria-label={
                        'Карточка XL, ' +
                        (hasInnerPaddings ? 'с внутренними отступами' : 'без внутренних отступов')
                    }
                    orientation="vertical"
                    size="l"
                    style={{
                        width: '13.6875rem',
                        flex: '0 0 auto',
                        alignItems: 'center',
                        gap: '0.5rem',
                    }}
                >
                    <div
                        style={{
                            position: 'relative',
                            width: '100%',
                            height: '8rem',
                            boxSizing: 'border-box',
                            padding: hasInnerPaddings ? '0 0.75rem' : 0,
                        }}
                    >
                        <ImagePlaceholder />
                        <div
                            style={{
                                position: 'absolute',
                                top: '3.5625rem',
                                left: 0,
                                right: 0,
                                zIndex: 1,
                                display: 'flex',
                                justifyContent: 'center',
                                overflow: 'hidden',
                            }}
                        >
                            <Numbers value={-28.8} unit="degree" size="xs" />
                        </div>
                        <IconButton
                            aria-label="Открыть карточку"
                            size="s"
                            style={{ position: 'absolute', right: 0, bottom: 0, zIndex: 1 }}
                        >
                            <IconPlasma color="inherit" size="s" />
                        </IconButton>
                    </div>
                    <BodyXS
                        style={{
                            width: '100%',
                            minHeight: '0.875rem',
                            overflow: 'hidden',
                            color: 'var(--text-primary)',
                            textAlign: 'center',
                            textOverflow: 'ellipsis',
                            whiteSpace: 'nowrap',
                        }}
                    >
                        Title
                    </BodyXS>
                </Card>
            );

            return (
                <Flow mainAxisGap="1.25rem" style={{ padding: '0.625rem', overflowX: 'auto' }}>
                    <ProductCard hasInnerPaddings />
                    <ProductCard hasInnerPaddings={false} />
                </Flow>
            );
        }
        ```
    </TabItem>
</Tabs>
