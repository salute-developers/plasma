import { styled } from '@linaria/react';

import { tokens } from '../../Attach.tokens';

export const IconWrapper = styled.div`
    line-height: 0;
    height: fit-content;
    padding: var(${tokens.moreIconPadding});
`;
