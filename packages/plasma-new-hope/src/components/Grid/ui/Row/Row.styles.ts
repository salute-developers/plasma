import styled from 'styled-components';

export const StyledRow = styled.div`
    display: flex;
    box-sizing: border-box;
    flex-wrap: wrap;

    margin: 0 calc(var(--plasma_private-grid-gap) * -1);
`;
