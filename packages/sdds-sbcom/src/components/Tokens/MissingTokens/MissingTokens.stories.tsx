// Generated by scripts/css-migration-tool.mjs — do not edit by hand.
// Regenerate with: node scripts/css-migration-tool.mjs update-storybook --package-root <path>
// (writes this file and missingTokensData.ts in Tokens/MissingTokens/)

import React from 'react';
import type { Meta, StoryObj } from '@storybook/react-vite';
import { InSpacingDecorator } from '@salutejs/plasma-sb-utils';

import {
    missingTokensByConfig,
    missingTokensByToken,
    missingTokensDescription,
    missingTokensGeneratedAt,
} from './missingTokensData';

const meta: Meta = {
    title: 'Tokens/MissingTokens',
    decorators: [InSpacingDecorator],
};

export default meta;

const tableStyle: React.CSSProperties = {
    width: '100%',
    maxWidth: '60rem',
    borderCollapse: 'collapse',
    fontSize: '0.875rem',
    lineHeight: 1.45,
};

const cellStyle: React.CSSProperties = {
    border: '1px solid var(--text-tertiary, #ccc)',
    padding: '0.5rem 0.75rem',
    verticalAlign: 'top',
};

const thStyle: React.CSSProperties = {
    ...cellStyle,
    textAlign: 'left',
    background: 'var(--surface-transparent-secondary, rgba(0,0,0,0.04))',
};

export const Default: StoryObj = {
    render: () => (
        <div
            style={{
                maxWidth: '72rem',
                boxSizing: 'border-box',
            }}
        >
            <h2 style={{ margin: '0 0 0.75rem' }}>Missing @salutejs/sdds-themes/tokens (from configs)</h2>
            <p style={{ margin: '0 0 1rem', lineHeight: 1.5, opacity: 0.9 }}>{missingTokensDescription}</p>
            <p style={{ margin: '0 0 1.5rem', fontSize: '0.75rem', opacity: 0.75 }}>
                Data generated: {new Date(missingTokensGeneratedAt).toLocaleString()}
            </p>

            {missingTokensByConfig.length === 0 ? (
                <p style={{ lineHeight: 1.5 }}>
                    No entries in missingTokensData. Regenerate with{' '}
                    <code style={{ fontSize: '0.85em' }}>
                        node scripts/css-migration-tool.mjs update-storybook --package-root [package-dir]
                    </code>{' '}
                    (from the repository root).
                </p>
            ) : (
                <>
                    <h3 style={{ margin: '0 0 0.5rem' }}>By config</h3>
                    <table style={{ ...tableStyle, marginBottom: '2rem' }}>
                        <thead>
                            <tr>
                                <th style={thStyle}>Config</th>
                                <th style={thStyle}>Token identifiers</th>
                            </tr>
                        </thead>
                        <tbody>
                            {missingTokensByConfig.map((entry) => (
                                <tr key={entry.configPath}>
                                    <td style={cellStyle}>
                                        <code style={{ fontSize: '0.85em' }}>{entry.configPath}</code>
                                    </td>
                                    <td style={cellStyle}>
                                        {entry.tokens.length === 0 ? (
                                            '—'
                                        ) : (
                                            <code style={{ fontSize: '0.85em' }}>{entry.tokens.join(', ')}</code>
                                        )}
                                    </td>
                                </tr>
                            ))}
                        </tbody>
                    </table>

                    <h3 style={{ margin: '0 0 0.5rem' }}>By token</h3>
                    <table style={tableStyle}>
                        <thead>
                            <tr>
                                <th style={thStyle}>Token</th>
                                <th style={thStyle}>Mentioned in configs</th>
                            </tr>
                        </thead>
                        <tbody>
                            {Object.entries(missingTokensByToken)
                                .sort(([a], [b]) => a.localeCompare(b))
                                .map(([token, paths]) => (
                                    <tr key={token}>
                                        <td style={cellStyle}>
                                            <code style={{ fontSize: '0.85em' }}>{token}</code>
                                        </td>
                                        <td style={cellStyle}>
                                            {paths.map((p) => (
                                                <div key={p} style={{ marginBottom: '0.25rem' }}>
                                                    <code style={{ fontSize: '0.85em' }}>{p}</code>
                                                </div>
                                            ))}
                                        </td>
                                    </tr>
                                ))}
                        </tbody>
                    </table>
                </>
            )}
        </div>
    ),
};
