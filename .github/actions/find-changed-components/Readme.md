# Find changed components for GitHub Actions

Этот скрипт определяет измененные компоненты и их зависимости для селективного запуска тестов в репозитории. Интегрирован как часть GitHub Actions workflow.

## 🎯 Назначение

Автоматически анализирует изменения в Pull Request и вычисляет минимальный набор компонентов, для которых нужно запустить тесты, включая:

-   **Прямо измененные компоненты**
-   **Компоненты, зависящие от измененных**

Это значительно ускоряет CI/CD пайплайн, избегая ненужного запуска всех тестов.

## Стратегии поиска изменений

Скрипт использует несколько подходов:

### Основной метод (GitHub Rebase)

```javascript
git log --oneline origin/dev..{HEAD_SHA} --pretty=format:"%H"
git show --name-only --pretty=format: {commit}
```

### Fallback метод

```javascript
git diff --name-only origin/dev...HEAD
```

## Поиск зависимостей

Поиск компонентов, которые используют измененные:

```javascript
// Паттерны для поиска
const importPatterns = [
  `const merged${component}Config`,
  `const ${component} = component(`
];

// Поиск в кодовой базе
grep -r "${pattern}" ./packages/plasma-new-hope/src/components
```

## Локальный запуск

```bash
# находимся в контексте .github/actions/find-changed-components
node index.js
```
