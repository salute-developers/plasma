name: Cypress component testing by schedule every day at midnight

on:
  schedule:
    - cron: 0 0 * * *

jobs:
  cypress_plasma:
    name: "Cypress for plasma packages"
    strategy:
      fail-fast: false
      matrix:
        scope: [web,b2c,ui,giga,homeds]
        browser: ['', 'webkit']

    uses: ./.github/workflows/cypress-common.yml
    with:
      scope: ${{ matrix.scope }}
      prefix: "plasma"
      browser: ${{ matrix.browser }}
      test-all: true
    secrets: inherit

  cypress_sdds:
    name: "Cypress for sdds packages"
    strategy:
      fail-fast: false
      matrix:
        scope: [cs,insol,os,finai,platform-ai,scan,serv]
        browser: ['', 'webkit']

    uses: ./.github/workflows/cypress-common.yml
    with:
      scope: ${{ matrix.scope }}
      prefix: "sdds"
      browser: ${{ matrix.browser }}
      test-all: true
    secrets: inherit
